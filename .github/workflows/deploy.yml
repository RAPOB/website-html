name: Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger cPanel Git deployment
        run: |
          curl -s \
            -H "Authorization: cpanel REDACTED:${{ secrets.CPANEL_API_TOKEN }}" \
            -X POST \
            "https://REDACTED-HOST/execute/VersionControl/create_deployment" \
            -d "repository_root=/home/REDACTED/repositories/website-html"
