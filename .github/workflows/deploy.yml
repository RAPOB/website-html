name: Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Update cPanel repo from GitHub
        run: |
          curl -s \
            -H "Authorization: cpanel REDACTED:${{ secrets.CPANEL_API_TOKEN }}" \
            -X POST \
            "https://REDACTED-HOST/execute/VersionControl/update" \
            -d "repository_root=/home/REDACTED/repositories/website-html&branch=main"

      - name: Deploy HEAD commit
        run: |
          curl -s \
            -H "Authorization: cpanel REDACTED:${{ secrets.CPANEL_API_TOKEN }}" \
            -X POST \
            "https://REDACTED-HOST/execute/VersionControlDeployment/create" \
            -d "repository_root=/home/REDACTED/repositories/website-html"
